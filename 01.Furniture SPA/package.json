{
  "name": "rest-service-task",
  "version": "1.0.0",
  "description": "Tasks with a local REST service",
  "scripts": {
    "test": "mocha tests",
    "start": "http-server -a localhost -p 3000 -P http://localhost:3000? -c-1"
  },
  "type": "module",
  "devDependencies": {
    "chai": "^5.1.2",
    "http-server": "^14.1.1",
    "playwright-chromium": "^1.49.0"
  },
  "dependencies": {
    "mocha": "^11.0.1"
  }
}
